spring:
  application:
    name: MentalHealthSystem

# 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/mental_health_system?useUnicode=true&characterEncoding=utf-8&useSSL=true&severTimezone=Asia/Shanghai&rewriteBatchStatements=true
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
# Redis配置
  data:
    redis:
      port: 6379
      database: 0
      host: localhost
# Mybatis-Plus配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl